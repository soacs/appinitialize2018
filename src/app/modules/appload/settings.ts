export interface AppSettings {
  connectionString: string;
  defaultImageUrl: string;
  businessName: string;
  primaryColor: string;
  secondaryColor: string;
  ngClass: string;
  ngStyle: string;
}
